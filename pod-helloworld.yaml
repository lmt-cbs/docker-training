apiVersion: v1
kind: pod
metadata:
  name: nodehelloworld.example.com
  labels:
    app: helloworld
spec:
  containers:
  -name: ks8-hellowworld
   image: lmtbelmonte/docker-training
   ports:
   -containerPort: 3000
