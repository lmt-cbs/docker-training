apiVersion: v1
kind: pod
metadata:
  name: nodehelloworld.example.com
  labels:
    app: hello-world
spec:
  containers:
  -name: ks8-hellowworld
   image: lmtbelmonte/docker-training
   ports:
   -containerPort: 3000
